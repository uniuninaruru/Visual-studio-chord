# Changelog

[**日本語**](CHANGELOG.md) | [English](CHANGELOG.en.md)

すべての注目すべき変更をここに記録します。日付は `Asia/Tokyo`。

## 未リリース

### 追加 — A/Bの判断が、生成そのものに反映されるようになりました

好みモデルはA/Bパネルができて以来ずっと学習していて、**生成物を一度も変えて
いませんでした。** `generator.ts` には preference への参照が一つもありません。
判断は「すでに引かれた5案の並び順」だけを変えていました。

モデルは作曲できません。特徴量は完成した曲の測定値（跳躍の頻度、7thの割合、
ボイシングの広さ）で、`melody.leapRate` の重みから生成設定への逆写像は存在
しません。**できるのは選ぶことです。** そこで複数案を引いてモデルが最も好む
ものを残します。best-of-n を、そう名乗った上でやります。

**学習に使っていないシードで測定** — これ以外に意味のある測定はありません。
「7thが多い方が勝ち」というA/B 20組で学習させたモデルで、保留した20シードの
7th率が **37.5% → 66.2%**、うち18件で先頭以外の案が選ばれました。

**「同じシードなら同じ曲」は壊れません。** 引かれる案はユーザーが与えたシード
から派生したシードで、勝った案は自分が引かれた派生シードを持ちます。学習済み
モデルで `abc` を生成すると `abc#3` というシードの曲が返り、`abc#3` は
モデルなしで、以後何を学習しても、まったく同じ曲を再現します。

**既定はオフで、何もしないことは特別扱いではありません。** 未学習のモデルは
重みもバイアスも持たないので全案が0点になり、同点は先頭に決まり、先頭は
入力されたシードそのものです。32小節8案で514ms かかるので、案の数は設定に
してあり、信用せず上限で切っています。

### 追加 — 見つけた進行を使えるようになりました／曲全体の解説

エンジンが既にやっていて、アプリが見せていなかったもの2件です。

**進行検索の「使う」。** 1500件を並べておいて、どれ一つ曲に入れられません
でした（試聴は先頭の和音を鳴らすだけ、パネルの `onApply` に呼び出し元なし）。
進行は度数の列であって長さの列ではないので、**小節は自分の和声リズムと
コードIDを保ち、各和音が何を綴るかだけが変わります。** 4ステップを6和音に
使えば循環します——4和音ループを8小節に使うのと同じことです。

置き換え先はボタンに書いてあります（選択中のセクション → 選択小節 → 曲全体）。
1小節のつもりで4小節のサビが書き換わるのは、事前に気づけない事故です。

書き換えられたセクションは記録していた進行名を手放し、カタログ上の名前が
渡されたときだけ受け取ります。派生バリアントはその名前で書かれたことがない
ので何も渡しません。これがないと、解説パネルが書き換え済みのサビを王道進行と
呼び続けます——**アプリが自分について嘘をつく**唯一の場所で。

**曲全体の解説。** `explainComposition` は各セクションの役割・出典の進行・
転調まで組み立てていて、どこにも表示されていませんでした。和音を選べばその
和音の説明が出て、何も選ばなければ何も出ませんでした。曲サマリーに、和音
パネルと同じ根拠付きの説明をセクション単位で表示します。散文形はコピー用に
用意しました（メモや質問に貼るのはそちらなので）。

### 追加 — 長い曲が落ちサビと大サビに届くようになりました

verseChorus は8区間で打ち止めでした（イントロ、2周、アウトロ）。**名前の由来
である形を書けませんでした** — 2周目のサビの後にブリッジが入り、サビをもう
二度、一度は落として、一度は最大で歌う形です。

11区間は4小節の下限を満たすのに44小節必要なので、この配置はアプリが持つ最長
の長さでしか選ばれません。これは制限ではなく主旨です。

両方ともサビの進行を構造的に借ります。**形が「一つの素材を三度違う設定で」と
言っているところに三つの進行を置けば、素材が三つになり、どれも「戻ってきた」
とは聞こえません。** 違いは全て種別ごとの表で決まるので、テストは「その表が
実際に違いを生んでいるか」を測ります。6曲でプール: 落ちサビは曲中で最も静か
（velocity 46、イントロ56に対して）、大サビは最も強く（90、サビ82に対して）、
伴奏の中心は 52.90 → 54.68 でその間にサビが入ります。

`allocateBars` の余り配分を、名乗っていた通りの最大剰余法に書き直しました。
「入る中で最も重い種に余り全部」は、最後の区間に流し込むのと同じ欠陥です。
11区間の形は余りが4小節で全種が切り捨て0になるため、**大サビが4小節を独占し
残る10区間はゼロ**でした。

うち2件のテストは最初1曲だけで書いていました。この長さでは種別あたり1区間なので、
**その測り方ではレジスタ表を別の数値に替えても通ります。** 6曲を読むようにし、
大サビのレジスタ項を潰すと 1.78半音の持ち上げが -0.29 になります。

### 修正 — 1小節のBメロは、Bメロではありません

16小節が `intro(1) verse(3) preChorus(1) chorus(3) verse(3) preChorus(1)
chorus(3) outro(1)` になっていました。**8区間のうち3つが1小節。** 楽曲形式では
なく、小節数の分割です。

下限は4小節、根拠はこのアプリ自身の `phrases.ts` です（4小節が前楽節と後楽節、
8小節が完全な文）。適用箇所は2つ——全区間が下限に届く配置しか選ばない、
その中で下限を先に配る——で、**両方必要でした。** 配置の制約だけでは重みが
下から再配分してBメロが2小節になり、重み付けを先にして後から修復するのは
もっと悪く、削る先が下限にいる軽い種そのものでした。

これが露出させたもの2件、どちらも今回の変更より古い問題です。

**サビは自分の音域に「到着」せず、登っていました。** 声部連結は前の和音から
測られレジスタの希望より重いので、引きは小節ごとに打ち勝つ必要がありました。
8曲のポップスで、サビの中心を自区間の小節ごとに測ると 50.3, 51.4, 53.8, 54.7
——**最初の2小節は前のBメロより下**で、区間が半分終わってから持ち上がって
いました。区間が重み付けされている間は見えませんでした（長いサビには登り
きった後の小節が残っていて平均を上げた）。等長にしたらその小節が消え、効果ごと
消えました。参照を破棄せず**レジスタの差だけ動かす**ので、「滑らか」は新しい
高さで意味を保ちます。サビ−Aメロの差は8スタイル全てで正（0.17〜1.74）。

**調推定が、旋律の音を含まない調を選んでいました。** Krumhansl-Schmuckler は
12個の数値との相関なので、スケール外の重みは少し損をするだけで失格になりません。
10曲中2曲——**臨時記号が一つもないハ長調の旋律がEナチュラルマイナー**、Fが17回
鳴っているのにFを持たないスケールです。候補を「鳴っている音のうちその調に
収まらない数」で並べてから相関で決めます。定数はなく、この基準が何も言えない
ときは従来の順序に完全に戻ります。10曲中0件になり、取り込んだ旋律の和音構成音率は
調を手で与えた場合と一致します。

**低音程限界を割る和音が3297中4件**、以前は0件でした。どれも同じ隅です:
**その和音の限界を守るボイシングは全て旋律を埋めます。** 1件を測ると、
クリーンな候補は12個あり最安でも旋律を埋めるコストが57.2、違反のコストは39.6
でした。テストは名乗れる主張——**区間レジスタは違反を増やさない**（オン・オフで
完全に一致）——に書き換えました。絶対値の0は、特定の曲についての事実であって
保証ではありませんでした。

### 調査 — クラスタ率と preChorus は、どちらも欠陥ではありませんでした

未解決として記録していた2件を追跡した結果です。**どちらもコード変更に至りませんでした。**
名指しした容疑者が無実だった、というのがこのセッションで3回目です
（ヒステリシス、数え方の変更に続いて）。

**クラスタ率 2% 対 参照 12%。** 疑っていたのは `cluster` の重み（スタイル別に
0.35〜1.8、私が5倍の幅で手設定したもの）でした。測ると:

```
クラスタを持つ候補が存在する和音  56%
勝者がクラスタを持つ            2〜3%

その候補が余分に払うもの（合計 15.85 のうち）
  melodyCovering +5.50 (35%)   density +4.48 (28%)
  topVoice +1.85   melodyClash +1.48   cluster +1.23 (8%)
```

**`cluster` は8%しか効いていません。** `maxSpan`、`bassCrowding` に続いて3つ目の、
測って実質不活性と判明したパラメータです。実際の抑制要因は melodyCovering
——これはボイサーが仕事をしている姿です——と `density`（`|声部数の差| × 2.8`、
プロファイル重みを持たない唯一の項）。

density を緩めれば数字は動きますが、参照コーパスは既に手元になく、声部数の分散に
ついて根拠がありません。**証拠なしに触るのは、このセッションで一度失敗した
「代理指標の最適化」と同じことです。**

**preChorus 専用テンプレート 0件。** これは欠陥として記録していましたが、
間違いでした。preChorus は `preChorus → verse → any` の順に引くので、専用の
テンプレートが無くても fall-through が働きます。40シード×2スタイルで測定:

| mode | intro | verse | **preChorus** | chorus | outro |
| --- | --- | --- | --- | --- | --- |
| major | 5 | 5 | **5** | 7 | 7 |
| naturalMinor | 7 | 6 | **7** | 8 | 8 |
| mixolydian | 9 | 9 | **9** | 5 | 5 |

**verse と同等です。** カタログ上の見た目の穴であって、生成される曲の多様性の
問題ではありませんでした。

Bメロ専用進行の裏取りも追加で行いましたが、基準（複数の独立ソースが同じ度数で
記述）を満たすものは見つかりませんでした。ある情報源が挙げるBメロ進行5件のうち
2件は既にカタログにある進行（王道進行・循環コード）で、残りと ii–iii–IV–V は
1ソースのみ、個別楽曲の採譜はこのカタログが明示的に除外しています。

### 修正 — 両手のボイシングが、両手だと言えるようになりました

伴奏が使っていない11半音の余地を追った先で見つかった、**このブランチで私が
入れたバグ**です。

追跡を終わらせた測定: 20半音以上あってメロディを完全に避ける候補は、popで69%・
balladで74%の和音に存在します。**メロディは止めているものではありません。**
その候補が代わりに払うのは clarity 5.47、spacing 4.20、topVoice 3.69、motion 1.12。

`spacingInversion` は音高だけから手の分かれ目を推測し、判定は「1オクターブ以上の
穴」です。`twoHandClose`——2オクターブ幅を埋めるためにこのブランチで追加した
シェイプ——の穴は**9半音**（左手ルート＋5度、右手はその1オクターブ4度上）。
9 < 12 なので単一の積みと判定され、**それを両手たらしめている穴を理由に**反転4を
課されていました。popで10、勝者との差14に対して。

推測ではなく宣言に変えました。カタログはどのシェイプが両手として作られているかを
知っています。両手として作られていないシェイプは影響を受けず、推測もそのまま
走ります。閾値を緩めれば1つを直すために全シェイプのスコアが動いていました。

**効果と、効果でなかったもの。** span中央値 29→28、低音程違反は全スタイルで0の
まま、メロディ埋没も不変。ただし ballad は大きく動きます——声部数 4.45→3.20、
両手率 75%→93%——これはクラシック参照（3.95声部）を**通り越して**おり、声部数に
ついて ballad は逆方向に間違うようになりました。チューニングで消さずに記録します。

選択側から引数1つを消す変異（メトリクスは直っているが使われない）が、そのファイルの
全テストを通過しました。配線もテストするようにしています。

### 追加 — アプリが、なぜそう書いたのかを説明します

エンジンは最初から知っていました。各和音がローマ数字・機能・借用元・解決先を持ち、
各セクションが元になった進行の名前を持っています。ほとんど表示していませんでした:
出荷デフォルトの171和音のうち **44%** が `explanation` を持ち、残りは何も持ちません
（`validateComposition` が非ダイアトニック和音にしか要求しないため）。あったものも
`"Slash chord: Em7 over B."` ——正しく、機械的で、**なぜそこにあるかを言っていない**。

保存せず計算します。有用なことを言うには曲全体が要る（どのセクションか、どの進行の
何番目か）ので、和音ごとのフィールドには見えません。曲に書き込めば曲が変わり、
曲の同一性は seed と設定であって自己記述の巧拙ではありません。

**各記述が出典を名乗ります。** これはこのアプリの他が守っている基準です——進行
カタログは独立ソースなしに何も収録せず、低音程限界は標準的なオーケストレーション表を
引用し、調推定は Krumhansl-Schmuckler を名指しします。理論を学ぶ人に見せる説明を
それより低い基準に置く理由はなく、最初の草稿は私の言い換えで、置いていました。

文献に当てて**2つの定義が変わりました**。トライトーン代理は「三全音を含む」では
ありません（ドミナント7thは全部そうです）。2つの和音が**同じ**三全音を共有し、
3度と7度が入れ替わるのが定義で、根音が主音の半音上に来ることがベースの半音下行の
出どころです。三機能は機能理論の言い方で述べます——安定、離脱、解決を求める緊張——
それぞれがどう感じるかではなく。

テスト前に間違えていた2つ: 「3和音の進行の**4番目**の和音です」と印字していた
（和声リズムが小節を分割するとセクションの和音数が進行のステップ数を超える）。
エンジンがラベルを付けなかった調外和音について**何も言っていなかった**。

読んでいて気づき、**直していないもの**: エンジンは ♭II7 をトニック機能に分類します。
説明はそれを忠実に報告していて、だから気づきました。

### 追加 — 1000件の進行と、その検索

**名前のついた進行は1000個存在しません。** `progressions.ts` は複数の独立した
実務ソースが同じ名前と同じ度数で記述するものだけを収録し、だから36件です。手で
1000件集めるには基準を捨てるか出典を捏造するかしかありません。

実証済みの技法を実証済みの進行に適用するなら届きます。サブドミナントマイナー、
セカンダリードミナント、トライトーン代理、パッシングディミニッシュ、7th化、
循環の回転——どれも独自の文献を持ちます。出自は必ず「このテンプレート＋この技法」。

```
major 539  naturalMinor 209  harmonicMinor 209  dorian 209  mixolydian 298 = 1464件
```

**名前は詐称しません。** 派生entryは元のidと技法を持ち、画面に「「◯◯」から派生」と
出ます。

技法は、それが名乗るものによって守られています。ここがこの方式の静かに失敗しうる
場所だからです。トライトーン代理は**I に解決する V にのみ**（iii の前の ♭II は
代理ではなく、名前に値しないクロマチック和音）、かつ**他がターゲットにしている V には
適用しません**（根音が三全音動くと手前が宣言したターゲットが消える）。
セカンダリードミナントは、既に接近中の2和音の間に割り込みません（巡回含む）。
**この種の欠陥を6件、読んでではなくテストが見つけました。**

検索は1行のテキストで、あらゆる読みを同時に試します: `4536`（度数）、
`IV V iii vi`（ローマ数字）、`王道`（名前）、`サブドミナントマイナー`（技法）、
`b2`（変化記号つき度数——♭II を ♭II7 と打つ人はいません）。各結果がどの読みで
一致したかを表示します。

**検索のバグ2件。** ローマ数字パーサが連結文字列で動いていて `IVmaj7 V7 iii7 vi` を
**4741** と読んでいました（区切りを落とすと数字が自己完結せず、`IV V` は `ivv` で
I-V-V とも読める）。トークン単位に変更。2文字の検索語が派生idの連番 `-2` に
誤ヒットしていたので、id照合は3文字以上に。

### 追加 — 短調が引ける進行が5件より多くなりました

長調29件に対し短調5件で、**短調のブリッジは何曲生成しても2択**でした。

4件追加。すべてこのファイルの基準——2つ以上の独立した実務ソースが同じ度数で記述——を
満たします: i–iv–v–i（4ソース）、i–♭VI–♭III–♭VII（2ソース）、
i–♭VII–♭VI–♭VII（2ソース）、i–♭VI–♭VII–♭III（小室進行の平行短調読み）。

**usage は全て `any`。** どのソースも「ブリッジ用」とは言っていません。tier を
埋めるために `bridge` と書くのは、このカタログが要求している証拠そのものの捏造です。

**短調ブリッジ 2 → 6**（40シード×32小節）。

tier の閾値も動かしました。「1件のtierを広げる」は定数を直してコイントスを残し、
短調カタログを厚くしたら今度は短調サビが2件で固定に。「2件超まで統合」で 2 → 8。
**長調はバイト単位で不変**（全tierが元から3件以上）、**ミクソリディアンは変わります**
（「短調だけ」という当初の見立ては不正確でした）。

### 削除 — 課金しようのなかったコスト項

`bassCrowding` は、最低音がベースから長10度より近いボイシングを罰していました。
**一度も課金していません**——`bassFor` を渡す呼び出し元がゼロでした。

配線しても直りません。`buildCompositionTracks` はベースを**和音自身の最低音**から
取るので、この項が測る差は `bassRegisterPitch` が動かす前なら0、動かした後なら
オクターブ単位。全候補に同じ値を足すだけで、探索としては何も決めません。

慣習自体は実在し、ベースが独立した声部のときに有効です。それはこのアーキテクチャでは
ない、とコメントに残しました。**「動きようがなかった」のか「考えが否定された」のか**を
次に不在に気づいた人が区別できるように。

私の診断が「bass +2.09」と出したのは、私が診断側でベースを渡していたからでした。

### 追加 — コードトラックが1種類のリズムしか持っていませんでした

ボイシングは「どの音を持つか」を決めます。**「いつ弾くか」は誰も決めていません**
でした。アルペジオを切ると、全スタイルの全和音が自分の小節頭で1回鳴って伸びるだけ——
240和音・240オンセット・1種類。入れても全部が同じレートの同じ走句でした。

`chordRhythms` は伴奏型のカタログです。慣習どおり小節頭からの拍数で書いてあります:
block / halves / pulse / offbeat（裏打ち）/ charleston / anticipation（食い込み）/
bossa（3+3+2）/ waltz / alberti / gallop。

```
和音あたりのオンセット数: pop 1.60  jazz 2.00  edm 3.24  rock 3.24  ballad 6.10
小節内の異なる打点位置: 2〜8（従来は全スタイル一律8）
```

**スタイルが選びます**（設定で名指しも可）。パターンを設定で指定させるのは、
ボイシング形状を指定させるのと同じ誤りです。拍子に合わない指定は、無理に鳴らさず
合うものへ落ちます——4/4のワルツはワルツではなく、3/4の3+3+2ボサは何でもありません。

**休符が図形そのもの**です。各打点は自分の長さで切れます。全部を小節末まで伸ばしたら、
それは打点の多いブロックコードです。

図形とアルペジオは同じ問いへの2つの答えなので片方だけ鳴り、図形が勝ちます
（より具体的な要求なので）。カタログには alberti・gallop という分散型が入っていて、
ballad は 6.10 オンセット——アルペジオが与えていた値と同じです。

### 修正 — ボイシングが一度も和音として鳴っていなかった

出荷デフォルトのコードトラックを8シード×16小節で測定したところ、
**オンセット1024回のうち同時発音は0回**、同時に鳴った最大音数は**1**でした。

アルペジオが既定で有効で、しかも各音を次の音の前に離していたためです
（216ticksの音を240ticks間隔）。つまり**すべてのボイシングが裸の単旋律として
鳴っており**、同時発音を読むコスト項——クラスタ、間隔の反転、和音内声部間の
低音程限界——は、決して鳴らないものを判定していました。

`ArpeggioSettings.sustain` は各音をペダルのように和音の終わりまで保持します。
音形が和音を置き換えるのではなく、和音へ積み上がります。

| | 3音以上が鳴っている時間 | 最大同時発音 |
| --- | --- | --- |
| 前 | 0% | 1 |
| 後 | **63%** | 4 |

同じ音を再度弾くときは前の音を離します。同一音の重なりは厚い和音ではなく
鳴りっぱなしの音だからです。既定はオフで、composition idも要求されたときだけ
変わるため、**すでにある曲の響きは変わりません**。

### 追加 — 伴奏がセクションに合わせて音域を動かす

`dynamics` は「サビはAメロより大きい」と主張しますが、**どこに手を置くか**に
ついての主張はどこにもありませんでした。測定すると、全スタイル・全セクション・
全曲で伴奏の最低音の中央値は **MIDI 43**——ひとつの音域で全部——であり、しかも
サビはAメロの **2.4半音下**にありました。

`SECTION_REGISTER` を追加しました。`SECTION_INTENSITY` と同じ形で、ブリッジが
Aメロより下に来る点も同じです（ブリッジは登るのではなく対比するので、サビより
高く到達したら、それが用意するはずの到達感を先に使ってしまいます）。
**規則ではなく引き**として書いており、メロディと低音程限界が桁違いに優先します。

測って直したことが2つあります。

**アンカーは中央Cではなく52です。** このアプリの伴奏はメロディ・ベース・
低音程限界の制約下で中心が52に落ち着くので、60を目標にすると**到達可能な候補が
一つもなく**、全候補に同じ定数が乗るだけで何も決まりません。実測すると60では
chorus−verse が −2.4 → −1.4 に動いただけで、曲全体の指標は完全に不変でした。
52では **+1.16**（3.5半音の振れ幅）になり、しかもAメロを下げるのではなく
**サビが上がります**。

**トレードは実在し、無料の点はありません。** オフセットを掃引すると、メロディの
埋没は弧とともに単調に増えます——静止時10.2%、0.4倍で11.3%、等倍で13.3%——
座るべき膝はありません。伴奏を上げればメロディに近づく。メロディが上にあるので
算術的にそうなります。等倍を採用し、**13.3%という数字をテストに書きました**。
後から発見されるのではなく、最初から明示されるように。低音程違反は8スタイル
すべて0のままです。

### 修正 — ピアノで最も普通の押さえ方が、生成できなかった

重み調整でボイシングがクラシックピアノの参照に一切近づかない原因を追ったところ、
**目標の幅の候補が生成されていない**ことが分かりました。3スタイルで測定した
候補の幅の分布:

```
提示: <12: 21%  12-17: 41%  18-23: 24%  [24-29: 0%]  30-35: 12%
```

2オクターブは参照——974曲、楽譜由来と実演——の幅の中央値であり、手が最初に
覚える形です。カタログはそれを作れませんでした。`twoHandVoicing` が右手を
ルートの**2オクターブ上に固定**するため作れるのは30以上、1オクターブ下なら
19〜22で、その間を橋渡しする形がありませんでした。

`twoHandClose` を追加しました。左手はルートと5度、右手は3度から始まる和音を
1オクターブ4度上に。**C3-G3 の上に E4-G4-C5**。幅が出るのは**転回であって移調では
ない**からで、ルートが手の下ではなく上に来ます。既存シェイプをずらしても
到達できない理由もこれです。

**直したことと、直さなかったこと。** 20-28半音の候補は全和音に存在するように
なり（94%→100%）、スタイルごとに3〜4回選ばれます。参照までの距離はスタイルに
より0.002〜0.025動き、1スタイルはわずかに悪化します。選ばれる幅の中央値は
**12のまま**です（この12を参照の24と並べるのは誤りでした。下記を参照）。

穴は必要条件で、まったく十分ではありませんでした。20-28の候補はコスト順位の
21パーセンタイルに座っており、候補の5分の1がまだそれより良い点を取ります。

**当初この記録は「4つの連結項によるヒステリシスが質感を止めている」と述べていま
した。測ったところ、そうではありませんでした。** `motion` `retention` `density`
`coherence` と `topVoice` を無料にしても、選ばれる幅は 13.55 → 14.09（pop）に
しか動きません。連結項を無視した最良候補は、実際の順位ですでに5〜6パーセンタイル
に座っています。連結項は勝者の平均コストのうち1.57（pop）で、残りは3.52です。

**そしてもう一つ、比較そのものが間違っていました。** 参照の幅24半音は独奏ピアノの
**テクスチャ全体**の値で、13.4半音はこのアプリの**伴奏だけ**の値です。同じ測り方
——全トラックまとめて——で比べると、このアプリは **29半音**、参照は **24半音**。
狭いのではなく**広い**のです。

伴奏がその幅に収まる理由は、測ると制約ですらありません。ベースの上端とメロディの
下端の間には **24〜25半音の余地**があり、伴奏はその **56%** しか使っておらず、
メロディの2半音以内に達するのは和音の **15〜22%** だけです。11半音が空いたまま
です。何がそこに留めているのかは、まだ分かっていません。

新しい形の中に**到達不能なガードが1つ**あったのも消しました。左手の上の間隔は
構造上どの品質でも最小7半音で、チェックは何も到達しない分岐でした。
mutationテストが素通りしたので気づきました。

### 追加 — メニュー（使い方・更新履歴・ライセンス・音量）

行き場のなかった4つ。使い方は初回チュートリアルとしてしか存在せず、一度閉じたら
戻れませんでした。版と版の間で何が変わったかの記載はどこにもなく、依存の
ライセンスは `package.json` の中だけ、そして**音量調整はどの階層にも存在しません
でした**。

パネルは中央ではなく左端から出ます。中身はすべて作業の**代わり**ではなく**傍ら**で
読むもので、フェーダーを動かす間も再生は続くからです。

ミキサーは全体・トラック別・リバーブ。分数で保持し、端で変換します。

- **フェーダーは2乗してゲインへ渡します。** 音量は対数的で、線形スライダーは
  travelの大半でほとんど何も変えません
- **0は本物の無音です。** −60dBは夜のヘッドホンではまだ聞こえるので、
  下げ切ったフェーダーは切れているべきです
- **プロジェクトの外に保存します。** 聴く音量は部屋とヘッドホンの性質であって
  曲の性質ではなく、書き出したJSONに入れたら開いた人に他人の音量が適用されます

トランスポートは適用先のシンセができるまで値を保持するので、初回再生前に設定した
音量は捨てられません。再生中に動かしたフェーダーも、ラベルだけでなくノードへ
届きます。**両方をテストしました。2つ目は、生きた経路を丸ごと削るmutationが
1つ目を通過させたからです。**

### 修正 — クラシックピアノへの較正を試して、値は一つも採用しなかった

Mutopia（楽譜由来）とMAESTRO（実演）の974曲——487曲で較正、487曲を保留——に対し、
`voicingDistance` を最小化する座標降下でプロファイルの72個の数値を探索しました。
**較正値は測定した上で破棄しました。** 分かったことのほうが価値がありました。

**重み調整では参照に届きません。** 保留参照が幅の中央値24半音・声部3.95・
2度の鳴っている時間12%・間隔の反転58%であるのに対し、このアプリは
29/4.47/2%/86% から 28/4.42/2%/87% へ動いただけです。合成距離は4〜9%下がり、
**聞き手が名前を言える性質は一つも動きませんでした**。出力をそこに留めているのは
重みではなく、候補生成器とコストの形です。`maxSpan` が一度もbindingしないという
発見に、逆方向から到達したことになります。

**代理指標の最適化は、強く押すほど危険です。** 距離は幾何10項しか見ず、メロディが
まだ聞こえるかを見られません。無制約で回すと距離は25%下がり、**メロディが伴奏に
埋もれる和音は10分の1から2分の1へ**増えました。`melody` の重みを固定しても
助けになりません——コストは重み付き和なので、隣の項が50倍に育てば固定した重みも
同じように格下げされ、埋没は3分の1まで達しました。全重みに上下限を設け、
メロディをより埋める候補を却下して初めて健全になり、その時点での改善が
「何も変えない4〜9%」でした。

**幾何は移りますが、テクスチャは移りません。** 出自を共有しない2つの参照が幅・
声部数・間隔で近く一致するので、幾何が記述しているものは実在します。しかしピアノ
独奏は自分でメロディを弾きます。和音構成音がメロディの上に来るのは、そこでは
ありふれた内声書法で、ここでは歌が埋まることです。

残したのは `voicingDistance`——次の試みが議論ではなく測定で決まるように——と、
`VOICING_PROFILES` に付けたコメント: **恣意的に見える関係が、測定によって
覆される機会を与えられ、覆らなかった**という記録です。

### 追加 — 遷移事前分布を、見たことのないデータで測る

この事前分布のこれまでの数値はすべて循環していました。分布は進行カタログから
数えており、このアプリが生成する4小節窓の30%はそのままテンプレート列なので、
出力を分布で採点することは「カタログに自分と一致するか尋ねる」ことでした。
1つ抜き交差検証がそれを取り除きます。

| | スコア |
| --- | --- |
| 訓練データ自身 | 0.351 |
| **保留（未知）** | **0.435** |
| ランダム列 | 0.698 |

前2つの差が循環そのもの、後2つの差が分布の実際の主張です。それが診断ではなく
**テスト**になったので、データを増やす・文脈を足すといった変更は議論ではなく
測定で判定できます。

きっかけとなった数え方の変更は、保留評価が「何の価値もない」と言ったので
元の挙動へ戻しました。

```
巡回1.0・正規化なし（採用）  0.270
巡回0.5                 0.268
テンプレート単位で正規化       0.268
両方                    0.268
巡回を数えない             0.258
```

サンプル規模に合わせた平滑化も同様です。保留の分離度は変わらないまま、最頻の
遷移と未観測の遷移の差が6.7から20へ広がりました——129観測が支える以上の確信度を
述べる事前分布です。既存テストも2件壊し、そのテストは正しかった。

理屈は妥当でしたが効果がなかったので、**測定上最良の設定のままオプションとして
残し、数値をコメントに記録**しました。同じ推論を最初からやり直さないためです。

### 修正 — 進行トラックでコードを選ぶと、停止ボタンが曲を返さなくなっていた

コードレーンで小節を選ぶとループがその小節に絞られ、■を押しても選択範囲の
先頭へ戻るだけで、曲の頭から再生できませんでした。`stop()` が選択を消さずに
ループを曲全体へ広げるようになりました。

### 変更 — CIをすべてのブランチで走らせる

CIは `main` へのpushとpull requestでのみ発火し、それ以外では走りませんでした。
そのため**pull requestを開く前に数日作業したブランチは、一度もチェックされません**。
実際、既定を有効化したことで壊れたブラウザ結合テストを載せたまま5コミットが
`feat/melody-aware-voicing` に積まれ、それを捕まえられた最初の実行は3日後の
pull requestでした。

どのブランチへのpushでも同じスイートが走ります。

pull requestは1つのブランチに対して両方のイベントを発火させます。**最初は重複を
concurrencyで打ち切りましたが、それは誤りでした**——打ち切られた実行もチェックを
コミットに報告するので、すべてのpull requestが本物の12件の隣に**キャンセル済みの
12件**を抱えることになります（このブランチで実測: 同一SHAに SUCCESS 12件、
CANCELLED 12件）。`gh pr checks` はキャンセルを失敗として数えるため、緑のpull
requestが半分赤く見えます。

そこで、gateジョブがそのブランチに開いているpull requestがあるかを尋ね、あれば
以降のジョブを**開始しません**。pull requestがスイートを走らせ、pushは何も足さない
ことを12件のキャンセルではなく1ステップで述べます。concurrencyはイベントごとに
分けてあるので、連続したpushは従来どおり互いを追い越します。

### 修正 — 公開版が、訪問者の持っていないサーバーの話をしていた

公開サイトを開くと、全員に「**ローカルAIサーバーは停止中です**」と表示されていました。
手元でDocker版を使う人には正確ですが、リンクを開いただけの訪問者は
ローカルAIサーバーを持ったことがありません。「停止中」は動いているべきものが
止まっている意味に読めるため、壊れている・自分で何か起動しなければならない、と
受け取られ得ます。

同時に、存在しないバックエンドを `/api/health`・`/api/device`・`/api/models` へ
**12回・14秒かけて探索**していました（バックオフで停止するので無害ですが無意味です）。

ビルド時フラグ `VITE_PUBLIC_BUILD` を追加し、公開ビルドでは次のようにしました。

- 探索を**1回も行いません**。接続状態は即座に確定します
- 接続理由に `browser-only` を追加。retry は `unreachable` のときだけ走るため、
  区別することで再試行が自動的に止まります
- 文言を「このアプリはブラウザの中だけで動いています。曲データはどこにも
  送信されません。」へ変更。訪問者にとって真である事実だけを述べます

**Docker版・ネイティブ版の挙動は変わりません。** バックエンドを起動し忘れた人には
従来どおり「ローカルAIサーバーは停止中です」と表示されます。ビルド成果物を確認した
ところ、公開版には旧文言も探索コードも残らず、通常版には新文言が入りません。

`frontend/tests/browserOnlyBuild.test.ts` に4件。早期returnの削除、文言の差し戻し、
reasonを `unreachable` に戻す、の3変異すべてでテストが落ちます。

### 追加 — インストール不要で、ブラウザから直接使えるようになりました 🎹

**<https://uniuninaruru.github.io/Visual-studio-chord/>**

**Dockerは要りません。ターミナルもZIPのダウンロードもコマンド入力も要りません。**
リンクを開けば、その場でコード進行とメロディが生成されます。スマートフォンや
タブレットでも同じリンクで動きます。

これまで、試してみたい人に最初にお願いしていたのは「Docker Desktopをインストール
してください」でした。曲を1つ聴いてみたいだけの人にとっては重すぎる要求で、
そこで離れてしまった方が多かったはずです。

**技術的には、最初からサーバーは不要でした。** 生成・編集・再生・MIDI書き出しは
すべてブラウザ内のTypeScriptで動いており、`useBackendConnection` はバックエンド
不在時に失敗ではなくブラウザのみへ縮退します。静的サイトとして配信すれば、
サーバーは1台も要りません。

サーバーを持たないことで、公開時に必要だったはずの対策が**修正ではなく消滅**します。

- プロセス共有の `PreferenceStore`（訪問者Aの応答に訪問者Bのコード進行が載る）
- フロントエンド公開時点で秘密でなくなる共有トークン
- レート制限の不在
- TLS終端

公開版で使えないのは、909曲の経験則モデルによる候補の**並べ替え**だけです。
A/B/Cの表示順が変わるだけで、**曲の中身は手元版と同じ**です。ニューラル和声は
もともとcheckpoint未同梱で使えません。

GPUやニューラル機能を使いたい方向けに、Docker版・ネイティブ版はこれまでどおり
残しています。


### 修正 — 指標名が実態と一致していなかった

`primaryMeanNormalizedNll` は名前に反して**正規化していませんでした**。単なる平均で、
しかも単位の違う量を混ぜていました — root / quality / bass / inversion は多クラス
cross-entropy（nat）、extensions だけが label 単位の binary cross-entropy です。
未学習時点で extensions の NLL は 0.048、root は 2.01 と桁が2つ違います。
この数値は `scripts/export-public-training-receipts.py` が**公開receiptに載せる**ため、
名前と中身の不一致が外に出ます。

- `meanActiveHeadNll` — 旧 `primaryMeanNormalizedNll` と**同じ値**を、実態どおりの
  名前で記録します。値を変えていないので、これまでの測定と比較できます。
- `meanNormalizedActiveHeadNll` — 旧名が約束していた正規化を実際に行います。
  各 head の NLL を、その head の語彙に対する一様分布の NLL（`log k`、
  extensions は binary なので `log 2`）で割ってから平均します。
- head ごとにも `normalizedNll` を記録します。

キー名を変えたのは、値の意味が違うものが同じ名前で並ぶのを避けるためです。
receipt exporter では新しい項目を**任意**にしてあり、この変更より前に作った
training run でも receipt を出せます。

実測 checkpoint での効果（8 epoch 目）:

| head | NLL | 正規化NLL |
| --- | --- | --- |
| root | 1.8805 | **0.7568** |
| quality | 1.4739 | 0.5316 |
| inversion | 0.3067 | 0.1905 |
| bass | 0.3273 | 0.1317 |
| event | 0.1020 | 0.0928 |
| extensions | 0.0404 | 0.0583 |

平均 0.2936 に対し、実際に学習が必要な root は 0.757。**自明に解ける4 head が
平均を押し下げている**ことが、正規化して初めて数字に出ます。

### 未修正 — 学習目的関数の宣言と実装の不一致

`configs/models/harmonyforge-bimask-base-v1.yaml` は
`objective: mean_normalized_active_head_cross_entropy` と宣言していますが、
`factorized_active_head_loss` は正規化しない単純平均で、extensions の
binary cross-entropy を同じ平均に混ぜています。**指標と同じ不一致が損失側にも
あります。**

こちらは修正すると学習挙動が変わり、既存の測定結果が比較できなくなるため、
今回は変更していません。判断は別途必要です。


### 修正 — 決定性の記録を宣言から観測へ

`training-run.json` の `deterministic` は、書き手が常に `True` を書き、読み手が
`True` を要求していました。**同語反復で、間違えようがない代わりに何も語っていません
でした**。重みではなくmanifestを公開し、第三者が同じhashを再計算して再現を検証する、
という配布方針はこの項目が実態を表していることに依存します。

- `deterministic` は、非決定的カーネルが実際に使われたかの**観測結果**になりました。
  保証の出どころはモードで異なります。厳格モードでは torch が例外を投げるため
  **完走したこと自体が証明**であり、`warn_only` では**警告が1つも出なかったこと**が
  証明です。どちらも実態から導出されます。
- `--allow-nondeterministic` を追加しました。決定的カーネルが無い演算で停止せず
  継続します。Apple Metal には決定的な embedding backward が無く
  （`index_put_with_accumulate_mps`）、このGPUで学習する唯一の方法です。
  **フラグは非決定的にするのではなく、非決定的でも進めることを許すだけ**で、
  実際にどうだったかは記録され、成果物の資格を決めます。
- **非決定的な run は `harmony_only_pretraining` しか作れません。** レシピからの
  再現を主張できない run は、公開される推論用成果物にはなれません。task境界と
  同じ形で、非決定的な重みは2つの独立したゲートで推論経路に到達しません。
- 環境側の警告抑制（`-W ignore`、`PYTHONWARNINGS`、pytestのfilterwarnings）が
  あると警告が消え、非決定的な run が「決定的」と記録されます。実機のMPSカーネルで
  この失敗を再現したうえで、`simplefilter("always")` で上書きしています。

`backend/tests/test_training_determinism_record.py` に12件。記録を定数へ戻す、
ガードを外す、厳格モードを消す、bool検査を外す、警告フィルタの上書きを外す、
の5つの変異すべてでテストが落ちることを確認しました。


### 追加 — 和声専用・非公開ローカル学習パイプライン

最初に学習する重みは、公開用のメロディ条件付きモデルではなく、**非公開・ローカル
限定の和声専用事前学習成果物**です。リポジトリはPOP909 corpus、normalized／processed
row、split assignment、学習済みweightのいずれも配布しません。利用者が本家から
自分で取得し、prepare・compile・学習をローカルで実行します。

**取得と抽出** — `scripts/fetch-pop909.py` は認証情報なしで本家GitHubリポジトリから
blob filter付きcloneとnon-cone sparse checkoutを行い、LICENSEと各曲の
`beat_audio.txt`／`chord_audio.txt`／`key_audio.txt` だけを実体化します。MIDI、音声、
アーカイブ、weight、正規化済み派生物は作業ツリーへ要求しません。
`scripts/prepare-pop909-harmony-only.py` は、調基準に正規化したコードroot・quality・
転回形・bass・extension、整数tickの和声リズム、key／mode、拍子、小節内位置だけを
出力します。メロディ、音声、歌詞、raw MIDI、演奏表現、voicing、編成、曲名などの
識別metadataは、読み込んでから捨てるのではなく**正規化の時点で出力しません**。

**compile** — dataset schema v2に `harmonyOnlyV1` content profileを追加しました。
`privateLocalHarmonyOnlyTraining` purposeでのみ到達でき、`privateLocalOnly`
distribution scopeに固定されます。provenanceは曲単位ではなくdataset／source subset
単位で記録します（安定したsource ID、version、正規URL、UTC取得日、citation、
source treeとnormalized inputの両方のSHA-256、実際にreviewしたcontent scope、判断根拠）。
`approved` はプロジェクト内の記録であって法的認定ではなく、`pending`、`blocked`、
出典不明、checksum不一致のsourceは学習へ入りません。ledgerがpreparation descriptorを
宣言する場合、`--prepare-run` でhashで束縛された `prepare-run.json` を要求します。

**原子的保存** — prepare・compileとも、途中で失敗しても直前の正常な一式を壊しません。
全出力をstagingへ書き、ディレクトリのrename1回で公開します。既存の非空ディレクトリは
そもそも上書き対象にせず、新しいversion付きディレクトリを要求します。renameの前に
staging、後に親ディレクトリを`fsync`し、ファイル内容も書き込み時に`fsync`するため、
「最終的な名前で公開されているが中身は page cache にしかない」状態を避けます。

**Docker build contextの監査** — 監査範囲を「Gitに入らない」から「Dockerのbuild送信領域にも
入らない」へ広げました。リモートDocker daemonではcontextがネットワーク越しに送信され、
cache layerがregistryへpushされ得るため、`COPY` を慎重に書くだけでは防げません。
生データ、学習済みweight、MIDI・音声素材を `.dockerignore` でcontext段階から除外し、
契約テストを追加しました。`scripts/check-private-artifacts.py` がCIと
`scripts/test.sh` の両方で境界を検査します。

**公開できるもの** — `scripts/export-public-training-receipts.py` は、非公開の
checkpointとcompile済みdataから、内容アドレス指定の束縛を検証したうえで
再構成不可能なreceipt JSONのみを書き出します。weight、split assignment、record ID、
source-item ID、raw content、ローカルpathは含みません。data card
（[日本語](docs/research/pop909-harmony-only-data-card.ja.md)）にrecipeとcontract、
固定checkoutに対する集計とhashを記録しました。

**未計測** — full neural trainingの所要時間、コスト、収束、音楽品質は測定していません。
data cardのsource review状態は、利用者が取得するrunごとに `pending` です。

### 追加 — 和声事前学習の重みを推論経路から締め出す安全境界

学習器は「メロディ条件付き可変リズム和声付け」に固定されています。和声だけで
作った事前学習の重みは、**同じアーキテクチャ・同じtokenizer・同じconfigを持つため、
既存の構造検査（tokenizer照合、architecture照合、全ファイルのSHA-256）を
すべて通過します**。両者を隔てるものは宣言された目的関数しかありません。
これを製品用AIとして読み込ませると、画面が表示する能力が実物と一致しなくなります。

穴は3つありました。

- **正直に宣言する語彙が無い。** `manifest.task` は
  `melody_conditioned_variable_rhythm_harmonization` の1値のみを許す `Literal` で、
  書き出し側もこの文字列をハードコードしていました。和声のみの重みは
  「メロディ条件付きだ」と名乗る以外にmanifestを書く手段がなく、
  スキーマが虚偽記載を強制していました。
- **境界がrelease-statusに相乗りしていた。** `MTC_ENABLE_RESEARCH_CHECKPOINT=1` は
  本番の推論経路へ `allow_research` として渡ります。「まだ評価していない」と
  「別の目的関数で学習した」は直交する軸なのに1つのフラグに畳まれており、
  環境変数1つで通常経路に入る状態でした。
- **表示に出ない。** backendのmanifest応答が `task` を含まず、
  クライアントは区別する材料を持てませんでした。

変更点。

- `manifest.task` に `harmony_only_pretraining` を追加し、事前学習の重みを
  正直に宣言できるようにしました。書き出し側（`save_trained_artifact` /
  `publish_checkpoint_manifest` / `train_reference_model`）も `task` を受け取ります。
- ローダは他のどのゲートよりも先に `task` を検査し、推論用以外を拒否します。
  **この判定は `allow_research` を経由しません。** 専用引数
  `permit_pretraining_task` は既定 `False` で、学習・書き出し・評価の各経路だけが
  明示的に渡します。推論経路（`TorchHarmonyBackend`）はこの引数に一切触れないため、
  **境界を開く設定項目も環境変数も存在しません**。
- 宣言された目的関数を `training-run.json` にも記録しました。同ファイルは
  manifestへSHA-256で連結されるため、目的関数がハッシュ検証の鎖の内側に入ります。
- `evaluate_checkpoint` は事前学習の重みも評価できます（それが昇格の手段であるため）。
  ただし返り値に `task` を含め、一方の目的関数で測った数値がもう一方の
  証拠として読まれないようにしました。
- backendのmanifest応答に `task` を追加。拒否された成果物は
  `available: false` と拒否理由に宣言された目的関数を表示します。

`backend/tests/test_harmony_pretraining_boundary.py` に8件。境界を削除する変異と、
判定を `allow_research` に畳む変異の両方でテストが落ちることを確認しました。
後者は軸の独立性を検証するテストが捕捉します。
### ドキュメント — READMEを初心者向けと技術者向けに分離

- 初めて使う人向けに、Docker、Apple GPU、Windows CUDA、Linux CUDA、
  スマートフォン接続を「どれを選ぶか」から順番に説明する導線へ変更しました。
- ターミナルをプロジェクトフォルダで開く方法、成功・失敗表示の見分け方、
  最初の曲を生成してMIDIを書き出すまでの操作、用語集を追加しました。
- 既存のGPU、ニューラルモデル、API、データ契約、セキュリティ、テスト、
  研究資料は削除せず、技術者向けリファレンスへまとめました。
- 英語READMEも同じ読者分離とOS別起動構成へ更新しました。

### 修正 — 候補採用ボタンが可視テキストで押せなくなっていた（`browser-e2e` 失敗）

候補カードの「この候補を採用」ボタンに追加された `aria-label` が、可視テキストを
含まない文言に置き換わっていました。`aria-label` はアクセシブル名を**上書き**するため、
ボタンの名前は `候補 A のプレビューを採用して履歴へ保存` となり、画面に見えている
`この候補を採用` では特定できなくなっていました。

- **WCAG 2.5.3「Label in Name」違反**です。音声操作の利用者が画面の文言どおり
  「この候補を採用」と言っても、そのボタンを操作できません。
- `browser-e2e` が3プラットフォーム（ubuntu/chromium、macos/webkit、windows/chromium）
  すべてで失敗していました。E2Eはボタンを可視テキストで探すためです。
- `aria-label` を `候補 A: この候補を採用して履歴へ保存` に変更し、追加の文脈を保ったまま
  可視テキストを含むようにしました。

**なぜ速いテスト層で捕まらなかったか**: ユニットテストが
`aria-label` に `"プレビューを採用して履歴へ保存"` が含まれることを検証していました。
つまり**文言そのもの**を固定しており、文言が変われば一緒に変わってしまう検証でした。
アクセシブル名が可視テキストを含むこと、という本来の契約を検証する形へ置き換えています。

### 修正 — インタプリタが消えた仮想環境をセットアップが再構築する（`e8546cc`）

`.venv/bin/python` はシンボリックリンクです。参照先のPythonが更新・削除されると
リンクが切れ、実在するパスなのに「file not found」で全スクリプトが失敗します。
XcodeやシステムのPythonが入れ替わったときに起こります。

セットアップを再実行しても直りませんでした。`venv` は既存ディレクトリに手を触れず、
**pipありでは異常終了、pipなしでは正常終了しながら何も修復しません**。後者は
終了コードを見るスクリプトに「成功した」と報告するため、より厄介です。

その後セットアップは次の検査へ落ち、
`The existing .venv uses an unsupported Python` と表示していました。
これは誤診です。環境は非対応のPythonを使っているのではなく、Pythonを持っていません。
どのバージョンを導入しても直らないため、読み手を無意味な作業へ誘導していました。

- `scripts/setup.sh` と `scripts/setup.ps1` は、インタプリタが無くディレクトリが
  存在する場合に `--clear` で再構築し、その旨を表示します。`--clear` が破棄するのは
  仮想環境だけで、プロジェクトには触れません。
- 再構築してもインタプリタが得られない場合は、Pythonのバージョンを責めるのではなく
  ディレクトリの削除を案内します。
- バージョン不一致のメッセージは、**動作するインタプリタが実在して版が違うときだけ**
  出るようになりました。
- `scripts/tests/test_setup_venv_repair.py` を追加しました。回避策が必要な理由である
  `venv` の挙動と、両スクリプトが再構築すること、メッセージの順序を固定します。
  終了コードはpipの有無で変わるため、**結果**で検証しています。どちらのスクリプトから
  `--clear` を外してもテストが落ちます。

**検証**: 実際に壊れた仮想環境を再現し、削除済みXcodeのPythonを指していた本
checkoutの `.venv` を修復しました。リポジトリルートから154テスト、`backend/` から
133テストが通過し、無関係なディレクトリからの `import app` も復旧しています。

## 0.4.0 — 大規模アップデート: HarmonyForge ニューラル和声研究プレビュー（2026-07-28）

この大規模アップデートは、研究計画を実行可能なモデル・artifact・API・
UI安全境界へ落とし込む基盤更新です。
**学習済みcheckpointや音楽品質評価結果は同梱しません。**
通常ユーザーは引き続き経験コーパスと決定的理論エンジンを利用できます。

### 追加 — HarmonyForge-BiMask

- 旋律、拍子、曲構造、編集mask、ロック済み和声を16分frameへ整列する
  決定的tokenizerを追加しました。
- 12層、hidden 768、12 attention heads、FFN 4096、pre-norm、GELUの
  single-encoder Transformerと、event / root / quality / inversion / bass /
  extensions / function / cadenceのfactorized headsを実装しました。
- 実装moduleは**104,567,874 parameters**です。v0.4はlearned window-position、
  bar・拍位置embedding、既存extension multi-hotのbiasなし8→768 projectionを
  使用し、rotary／relative attentionは将来の比較実験です。
- 1回のforwardは1 tokenizer window（batch size 1）を処理し、1〜32候補を
  同じlogitsからseed付きsamplingします。候補数は実行batch sizeではありません。
- 同一checkpointをCUDA、Apple Metal/MPS、CPUで使用します。実tensor probe、
  dtype選択、device provenance、acceleratorからCPUへの明示fallbackを追加しました。
  v0.4はOOM時のbatch縮小を行いません。

### 追加 — 厳格なartifactとAPI v2

- `manifest.json`、`data-manifest.json`、
  `harmonyforge-bimask-base-v1.safetensors`を固定配置からだけ読みます。
  architecture、config/checkpoint/data-manifest実fileのSHA-256、固定tokenizer
  digest、training/evaluation status、PyTorch/app/API version、precisionを
  読み込み前に検証します。compilerはexport前にledgerとsplit / vocabulary /
  statistics artifact hashも検証し、dataset権利・leakage評価は別のtraining gateです。
- `POST /api/v2/harmony/generate`、`GET /api/v2/jobs/{requestId}`、
  `POST /api/v2/harmony/cancel/{requestId}`、
  `GET /api/v2/models/{modelId}/manifest`を追加しました。
- jobは候補をまとめて公開し、cancel、timeout、checkpoint拒否、推論失敗では
  partial candidateを残しません。request ID、seed、model/checkpoint/deviceを
  provenanceとして返します。
- research-only checkpointは`MTC_ENABLE_RESEARCH_CHECKPOINT=1`を要求します。
  開発用fixtureは`MTC_ENABLE_NEURAL_MOCK=1`でのみ有効になり、UI/APIの両方で
  `MOCK`、`trained: false`、`notEvaluated`として区別します。

### ユーザーワークフローと安全性

- ChordLaneで範囲を選び、部分再生成の「コードのみ」とAuto / MPS / CUDA / CPUを
  選ぶと、A/B/C用の3候補をbackground jobで要求します。推論中も再生と手動編集を
  継続でき、状態バーから進捗、経過時間、実device、fallback理由を確認し、
  Cancelできます。accelerator失敗後は同じ範囲を「CPUで再試行」できます。
- server候補は`hardRuleValidation: pendingClient`、
  `adoptable: false`で届きます。既存のschema、理論、voicing、88鍵・左右手、
  全track validatorをclientで通過した候補だけをpreviewできます。
- previewを試聴して明示的にApplyしたときだけ正式projectへ反映し、Undoできます。
  互換editへrebase・再検証した候補は`Rebased`と表示し、文脈が変わったstale結果、
  Cancel、失敗は編集中の曲を変更しません。
- 学習済みHarmonyForgeがない場合は、経験コーパス、ブラウザ軽量ranking、
  決定的理論生成へ安全にfallbackします。

### 配布・CI・既知の限界

- 標準backend lockとCI laneはPyTorch / SafeTensorsを含まないtorch-free構成を
  維持しました。別のneural CPU laneが固定依存でモデル構築、checkpoint拒否、
  API/cancel/mock/preview契約を検査します。
- CPU / CUDA / macOS acceleration lockへPyTorch 2.13.0とSafeTensors 0.8.0を
  固定しました。DirectMLはv0.4 HarmonyForge deviceではなくONNX ranker用です。
- 通常の`setup.sh` / `setup.ps1`からも固定済みPyTorchを導入し、macOS MPS、
  NVIDIA CUDA、CPUを実tensor probeで自動選択します。CUDA/MPS失敗はCPUへ
  fallbackし、Browser / Theory-only起動は常に残します。
- CPU Dockerはneural runtimeを含みますが、validな学習済みcheckpointを
  read-only mountした場合だけHarmonyForgeを利用できます。CUDA Compose overlayは
  NVIDIA driverとNVIDIA Container Toolkitを別途必要とします。
- 学習data compiler、closed test、ablation、CUDA/MPS/CPU同等性試験、
  聴取実験は今後の研究gateです。mockやparameter数は音楽品質の証拠ではありません。
- 主要参照はAutoHarmonizer、ReaLchords、Stochastic Control Guidance、
  full-to-full curriculum maskingです。アーキテクチャ図、採用した考え方、
  独自integration、完全な一次資料一覧を
  [日本語](docs/neural-harmony-architecture.ja.md) /
  [English](docs/neural-harmony-architecture.en.md) に分離しました。

## 0.3.0 — 大規模アップデート: Research-grounded Engine / Empirical Harmony（2026-07-27）

このリリースは「開発者の感覚で重みを足す」方式から、理論制約・実測統計・
ユーザー評価を分離した生成エンジンへ移行する大規模更新です。パッケージ、
フロントエンド、バックエンドのバージョンを `0.3.0` に揃えました。

### 設計 — 先行研究に基づくニューラルコード生成（次期0.4.x）

- 2024〜2026年の旋律条件付き和声付け、可変ハーモニックリズム、和声token化、
  非微分可能rule guidance、低遅延伴奏の一次資料を比較しました。
- 主モデルを、旋律と和声を同じ時間軸で扱うsingle-encoder masked Transformer
  `HarmonyForge-BiMask` としました。decoder-only、大規模audio-token生成、
  学習前MLPは主経路にしません。
- root、quality、inversion、bass、extensionsをfactorized headsで出力します。
  flat vocabularyや別token化が優れる可能性は残し、ablationで比較します。
- 同じtokenizer、architecture、checkpointをCUDA、Apple Metal（MPS）、CPUで
  使用します。デバイス別モデルを作らず、precisionとbatch方針だけを変えます。
- ニューラル出力は既存のハード理論制約、88鍵・左右手voicing、全track検証を
  通過したpreviewとしてのみ返し、ユーザーのApply前に本編を上書きしません。
- test leakage、データ権利不明、MPSのsilent CPU fallback、既存engineより低い
  人間選好のいずれかが残る場合はreleaseしない停止条件を定義しました。
- 実装前仕様を
  [日本語](docs/research/neural-chord-model-plan.ja.md) /
  [English](docs/research/neural-chord-model-plan.en.md)、
  先行研究・最先端比較を
  [日本語](docs/research/neural-harmonization-sota.ja.md) /
  [English](docs/research/neural-harmonization-sota.en.md)
  で追加しました。初期configは
  `configs/models/harmonyforge-bimask-base-v1.yaml` です。

### 追加 — 909曲から学習した経験則モデル

- POP909の909曲をローカルで解析し、1,131の調性区間、93,904コードトークン
  から1〜5次の調和言語モデル `harmony-corpus-ngram-v1` を構築しました。
- ルートを曲の絶対キー名ではなく「現在キーからの半音差」に正規化し、
  コード品質と組み合わせます。同じ機能の移調曲を別物として数えません。
- 転調点では系列を分割し、同じコードが注釈上分割されているだけの場合は
  1イベントへ統合します。
- 実行時は文脈の観測回数から補間率を決める再帰的n-gram確率を使用します。
  ジャンルらしさを作るための未出典な小数重みは足していません。
- 候補生成後のランキング層へ接続しました。終止、適用和音の解決、
  声部交差などのハード制約を統計モデルが無効化することはありません。
- モデルがない、壊れている、schemaが未知の場合は曲を変更せずTheory-onlyへ
  フォールバックします。
- `auto` は学習されていないMLP/ONNXより、実測コーパスモデルを優先します。

### 追加 — 再学習可能なモデル境界

- `scripts/train-harmony-corpus.py` を追加しました。ローカルのPOP909 checkout
  または任意JSONLから1〜8次モデルを再学習できます。
- 書き込みは一時ファイルからのatomic replaceで行い、失敗時に既存モデルを
  半端な内容へ上書きしません。
- 追跡モデルは集計n-gramだけを含みます。MIDI、音声、歌詞、曲名、元注釈は
  リポジトリへ複製しません。
- `MODEL_DIRECTORY` は相対・絶対パスのどちらも使え、アプリ側のモデル容量上限を
  撤廃しました。巨大なニューラルモデルも同じ境界へ追加できます。
- Docker backendへ `./models` をread-only mountし、macOS、Windows、Linuxで
  同じモデルIDを使用します。

### 変更 — デスクトップとスマートフォンの役割

- デスクトップPCを保存・学習・CPU/GPU推論を担当するローカルホスト、
  スマートフォンを同一LANから接続するブラウザUIとして明文化しました。
- スマートフォンでもデスクトップ上の同じ経験則モデルで候補を評価します。
  モデルをスマートフォンへ複製する必要はありません。
- 狭い画面はSettings / Inspectorをドロワー化し、Transport、再生、簡易生成、
  候補評価を優先します。これは音楽エンジンの簡略版ではなくUIの役割分担です。
- 今後の開発では容量、モデルサイズ、クライアント計算量を主要な採否基準にせず、
  音楽品質と再現可能な検証結果を優先します。

### 変更 — 研究根拠を持つ和声探索

- 機能和声の擬似的な小数コストを序数の選好段階と辞書式比較へ置換しました。
- コード種を各スロットで抽選せず、曲区間全体を探索して終止、実根音の解決、
  ボイスリーディング、方向性を検査します。
- Neo-Riemannian P / L / Rを実際のコード候補として生成し、変換元、操作、
  品質を保存・再検証します。
- Tonnetzの辺数を万能距離として使わず、同声部数の実音高に対する最小総移動量を
  ボイスリーディング距離に使用します。
- 根音の方向、実ベースの方向、共通音損失、クロマティック連続数を別々に保持し、
  出典のない1個の「良さスコア」へ潰しません。

### API・互換性

- API v1にbackend種別 `corpus` とmodel ID
  `harmony-corpus-ngram-v1` を追加し、OpenAPI由来TypeScript型を再生成しました。
- API envelopeとプロジェクトschema v2は変更していません。既存プロジェクトの
  migrationは不要です。
- コーパス用の調性正規化特徴を追加したため、候補特徴schemaをv2へ更新しました。
  既存の疎な好み重みは名前一致する特徴だけ継続利用できます。
- Diagnosticsでコーパスモデルの存在、load状態、CPU runtime、fallback理由を
  確認できます。
- ブラウザ側の実レスポンス検証も`corpus`を認識し、未知backendは従来どおり
  安全にAPI不一致として拒否します。

### 検証と既知の限界

- 学習器の移調正規化、転調分割、重複統合、atomic保存を単体テストしました。
- ランタイムのschema拒否、load/unload、既知文脈が未知文脈より高くなること、
  モデル欠落時Theory fallbackをテストしました。
- このモデルは「候補の経験的な自然さ」を評価します。旋律そのものやコード候補を
  ニューラル生成するモデルではありません。
- POP909由来なので、現段階の経験分布は主にポピュラー音楽です。古典対位法、
  ジャズ、ゲーム音楽を同じ精度で代表するとは主張しません。
- 個人のLike / Dislike学習はコーパス尤度へ加算されますが、ハード理論制約より
  前には移動しません。

### 追加 — 88鍵・DAWトラック・両手ボイシング・全トラック検査

- メロディ音域をA0〜C8の88鍵全域から選べるようにしました。ピアノロールは広い音域で縦スクロールし、狭い音域では従来の表示密度を保ちます。
- コードを最低音の **Bass / Left Hand** と上声の **Chords / Right Hand** に分離し、主旋律と追加声部を含むDAW型トラック一覧を追加しました。
- 各トラックを単独表示でき、`V`（表示）、`M`（ミュート）、`S`（ソロ）を切り替えられます。ミュート／ソロは表示だけでなくTone.js再生にも反映されます。
- MIDIを **Bass / Left Hand、Chords / Right Hand、Melody、追加声部** の3本以上の独立トラックとして出力します。
- 全トラック同時再生時の88鍵範囲外、左右の手の交差、低音域の密集、主旋律の未解決不協和、対旋律の禁則、カノンの衝突を検査する最終品質ゲートを追加しました。
- カノンは現在の和音と主旋律の双方に対して安全な音だけを鳴らします。
- コードの彩りを「シンプル／豊か／カラフル／冒険的」から選べる設定を追加し、クロマティックな和音が3つ以上連続しない制約を追加しました。
- Auto Fixは7thと機能和声探索を提案し、修正後に全トラック品質ゲートを実行します。

### 修正 — 旋律の和声適合と音域

- 強拍とフレーズ端をコードトーンへ寄せ、経過音・刺繍音など前後関係で説明できない不協和音を決定論的に修復します。
- 大跳躍後は反対方向の順次進行を優先し、フレーズ頂点と音域の弧を考慮します。
- 既定のメロディ音域をG3〜E6へ拡大しました。
- 45種類のキー／モード／シード、2,000音以上で未説明の強拍非和声音、大きすぎる跳躍、跳躍後の未回収を回帰テストします。

### 追加 — Phase A〜D 設定UI・多声部・Auto Fix mode

- Advanced設定を「曲の流れ」「歌えるメロディ」「和音を豊かに」「ノリと多声部」の4段階へ整理し、Phase A〜Dの生成設定を画面から選択可能にしました。
- `GeneratedComposition.voices[]` を追加し、対旋律・カノン・ポリリズム低音を後方互換な追加声部として保存します。従来の`notes[]`は編集可能な主旋律として維持します。
- 追加声部をTone.jsで再生し、ピアノロールで色分け表示し、声部ごとにミュートできるようにしました。
- MIDIはChords／Melody／各追加声部を別トラックで出力します。
- プロジェクトschemaをv2へ更新しました。v1とschema番号がない旧ファイルは読み込めますが、未知の新しいschemaは安全に拒否します。
- Auto Fix modeを追加しました。現在の曲を変更せずに決定論的な修正案を作り、理由・検証結果・対旋律チェックを確認してから適用できます。適用操作は履歴へ入り、Undoできます。

### 追加 — 高度リズム・対位法（Phase D）（`3de354f` `c00be0e` `f622157` `784785d`）

リズムと声部数を扱えるようにしました。

- **ユークリッドリズム（`euclideanRhythm`）**: 指定数の音を等間隔に配置します。Bjorklund のアルゴリズムを実装したので、トレシージョ E(3,8)、シンキージョ E(5,8)、ボサノヴァ E(5,16)、西アフリカのベルパターン E(7,12) などが、音楽的な入力なしに得られます。各音は次の音まで持続します（打点後の沈黙はその打点に属します）。
- **グルーヴテンプレート（`groove`）**: 「どの音を弾くか」ではなく「どう弾くか」。小節グリッド上の遅延カーブと強弱カーブの2本で表現し、ストレート／スウィング2種／シャッフル／ボサ／レイドバック／プッシュ／バックビートの8種。カーブは tick でなく1ステップの比率なので、PPQ と拍子が変わっても同じ意味になります。
- **対旋律（API）**: 平行5度・平行8度・声部交差を避け、反行の度合いを指定できる第2声部。6曲414音で平行完全音程・声部交差ゼロ、不協和は4.3%（経過的なもの）。independence を 0→1 にすると反行が 41%→59% に増えます。
- **カノン／模倣（API）**: 旋律の遅延・移調・反行。曲末を越える音は折り返さず落とします。
- **ポリリズム／ポリメーター（API）**: 同じ長さを異なる数で割る層と、拍を保ったまま小節長を変える層。両者が揃うまでの小節数（7対4なら7小節）を先に問い合わせられます。

**実装中に見つけて修正したバグ**:
- 対旋律で、音域が旋律を越えられない位置では音を1つ飛ばしていた。2声は添字で対にするため、1つの欠落が以降のすべての比較を静かにずらしていた

**自分のテストの誤り**: 6ステップのパターンが4ステップ小節に戻るのは3小節後（2小節目は「繰り返し」であって「小節線上での再開」ではない）。実装が正しく、テストが誤っていた。

**更新**: 追加声部は現在、再生・MIDI書き出し・ピアノロール・プロジェクトschemaに接続されています。

### 追加 — 高度和声（Phase C）（`6f50e0c` `0085fcc` `4d87502` `a89aad0` `d3785b4`）

和声の語彙と、それを読むための解析を追加しました。

- **ピボット和音転調（`pivotModulation`）**: 両キーにダイアトニックな和音を探し、転出セクション最後の和音を書き換えて継ぎ目を準備します。新キーの下属和音を最上位に置く順位付けで、C→G は Am（vi=ii）を選びます。共通音のないキー（C→F#）は無理にピボットを作らず直接転調に落とします。
- **モーダルインターチェンジ体系化（API）**: 平行スケール群（教会旋法7つ＋和声的短音階・旋律的短音階）から借用語彙を構成。C メジャーで46和音、うち名前を持つ借用は4つの異なるスケール由来です。スタイル別の重みが pop=iv、rock=bVII、jazz=iiø7 と分離します。
- **コードスケール理論（API）**: 15スケールと照合し、利用可能音・アヴォイドノート・到達できるテンション・キーからの逸脱数を報告。長調で Imaj7→Ionian、ii7→Dorian、iii7→Phrygian、IVmaj7→**Lydian** という教科書解を無指定で返します。
- **リハーモナイゼーション（API）**: 既存メロディに対し9技法で候補を生成し、旋律適合・機能的進行・スタイル適合で採点。導音を旋律に持つ G7→C で、jazz は Db7（裏コード）を首位に、pop は候補に出しません。
- **クロマティックメディアント／対称和声（API）**: 三度関係の和音を共通音数で分類（ダイアトニック2／クロマティック1／二重クロマティック0）、全音音階・オクタトニックとその和音を算出。

**実装中に見つけて修正したバグ**:
- ピボットの減点を「機能が tonic」で判定していたため、トニック代理の vi/iii まで減点していた（vi は C→G の教科書的ピボット）
- ダイアトニックな7thコードが「借用」として提示されていた（native 集合を三和音のみで構成していたため、C メジャーで Fmaj7 が外部扱い）。候補53→46
- `borrowingsForDegree(4)` が III 上の和音を IV の借用として返していた（長調で IV の半音下は III）
- コードスケールの適合度でアヴォイドノートを減点していたため、Em7 が Dorian になっていた（C の曲に F#/C# が乗る）。アヴォイドノートは報告するもので採点するものではない
- 裏コードの解決を過小評価していた（属七の半音下行は五度下行と同等に強い進行）

到達不能な型（共通音1の規則下では `modeRelation: "mixedQuality"` が発生しない）を発見し、共通音0の「二重クロマティックメディアント」を加えて意味のある区分にしました。

### 追加 — 旋律の骨格・非和声音・ガイドトーン（Phase B）（`c2dd755` `c0363d7` `d86bc97`）

旋律を「1音ずつ正しい」から「フレーズとして意味を持つ」へ変えました。

- **Melodic Skeleton（`melodicSkeleton`）**: フレーズごとに開始音・頂点を、終止するフレーズにはさらに準備音と解決音を先に決めます。打点の固定だけでは形にならなかったため、計画を音域として補間し全音を評価対象にしています。24シードで、計画音域と実際の平均音域の相関が 0.06→0.49（23/24シードで改善）。
- **非和声音パターン（`nonChordTones`）**: 8種の図形を「準備→不協和→解決」の3イベント一組として生成。装飾は既存の音の時間内に書き込まれるので、線が占める時間は変わりません。
- **ガイドトーンライン（API）**: 各和音の3rdと7thを担う2声を、同時に最短経路で解きます。

**実装中に見つけて修正したバグ**:
- ガイドトーンラインを「3rdの線」「7thの線」として独立に解いていた。ii–V–I で滑らかにつながるのは 7th→3rd の解決であり、声部は担う構成音を交替する。修正で順次進行・共通音の割合が 0.14→0.77
- 囲い込みが全図形の44%を占めていた（前提条件が緩すぎた）。跳躍要件と頻度重みを入れて15%に是正、経過音は8%→21%

**出荷しなかったもの**: ガイドトーンラインの再ボイシングは削除しました。60曲・1129遷移で測ると改善0・悪化3・不変1126、それでいて98%の和音を書き換えており、既存の4声ボイサーが既にほぼ最適だったためです。12シードでは通っていたテストも、偶然と判明したためコードごと削除しています。

### 追加 — 和声リズム・フレーズ文法・機能和声・4声体・緊張カーブ（Phase A）（`f5acd97` `890fce9` `5e5758f` `4fb7a88` `ad45689`）

「1小節1コードの上に単旋律を載せる」制約を外し、時間・声部・構造の3方向を与えました。

- **和声リズム（`harmonicRhythm`）**: コードを小節ではなく tick スロットに配置。1小節に複数回変わる進行も、複数小節保持する進行も表現できます。終止に向けた加速も指定可。
- **フレーズ文法（`phraseGrammar`）**: 古典的な楽節構造（提示→応答→断片化→カデンツ）。各フレーズが終止の強さと頂点位置を持ちます。
- **機能和声（`functionalHarmony`）**: 進行を状態機械として定義し、動的計画法で最短経路を解きます。
- **4声体（`voiceLeading`）**: SATB の声部配置と、連続5度・連続8度・声部交差・限定進行音の未解決の検出。スタイル別の重みで、同じ8小節に対し `game-music` は完全協和音程の並行0個、`edm` は6個。
- **緊張カーブ／エネルギー曲線（API）**: 既存の音楽から測る緊張度と、セクションごとに計画するエネルギー。

**実装中に見つけて修正したバグ**:
- 機能和声の遷移表から `predominant→resolution`（変格終止）と `tonicProlongation→tonic`（ループ終止）が抜けており、その2つの終止形が到達不能だった
- `ChordLane` が「1小節1コード」を前提に最初の和音だけを描画していたため、和声リズムを細分化すると残りが黙って消えていた

**既知の弱点**: 緊張カーブのリズム成分は既定の音符密度でほぼ全小節0.8超で飽和しており、判別にほとんど寄与していません。

**Phase A〜D 共通の検証**: すべての新機能は `GeneratorSettings` の任意フィールドで、指定しなければ従来と同一の出力です。各コミットでキー×モード×小節数×スタイル×シードの360曲を親コミットと本コミットの両方で生成し、バイト単位の一致を確認しています。572テスト（Phase A〜Dで新規約350件）、lint・typecheck・build すべてクリーン。主要モジュールはミューテーションテスト済みです。

**更新**: 生成設定は現在Advanced画面から利用できます。解析専用APIはAuto Fixの診断または開発者APIとして利用します。

## 2026-07-25

### 追加 — 曲構造・転調・複調・ペンタトニック（`5131e1a`）

コード進行の実装（下記）を「進行そのもの」と「曲の構造」に切り分け、後者を実装しました。

- **セクション（`SectionEvent`）**: 曲を Intro / Verse / Pre-chorus / Chorus / Bridge / Outro の区間に分割し、区間ごとに固有のキー・モード・コード進行を持たせられるようにしました。セクションは既存のフラットな `chords[]` / `notes[]` 配列を「指し示す」だけなので、再生・MIDI書き出し・ピアノロールは無改修で動作します。
- **曲構造（`GeneratorSettings.songForm`）**: `verseChorus` / `aaba` / `throughComposed` の3形式。反復形式（Verse-Chorus・AABA）は同種セクションで進行を再利用し、通作形式はセクションごとに異なる進行を選びます。小節数4/8/16のいずれでも過不足なく割り付けます。
- **転調（`finalLift`）**: 最終セクションのみを指定半音数だけ移調する「トラックドライバー」型のキー変化。
- **複調（`polytonal`）**: メロディを和声の平行調で生成するオプション。
- **ペンタトニック旋律（`melodyScale`）**: ヨナ抜き（4度・7度抜き）／ニロ抜き（2度・6度抜き）音階を、親モードから導出してメロディにのみ適用。
- **進行選択の決定性対策**: セクションのコード進行は配列の並び順ではなく、進行IDのハッシュ値で選択するよう設計。将来カタログに進行を追加しても既存シードの出力が変わりません。

**実装中に見つけて修正したバグ**:
- 転調セクション内の和音が、検証器が曲の開始キーで判定していたため全て無効と誤判定されていた
- 部分再生成（`regenerateRange`）が転調セクションを曲の開始キーで再生成し、そのセクションに属さない和音を生成していた
- AABAの3つのAセクション、Verse-Chorusの2つのサビが、それぞれ別々の進行になっていた（反復形式の定義に反する）

**検証**: 213テスト（新規35件）が通過。キー×モード×小節数×スタイル×シードで360曲を本コミットと親コミットの両方で生成し、バイト単位で完全一致することを確認（既存の楽曲は1音も変わりません）。`songForm` を指定しない限り `sections` フィールドは出力されず、旧形式のプロジェクトファイルはそのまま読み込めます。

現状、これらの新機能は `GeneratorSettings` の指定でのみ利用できます。Basic/Advanced設定UIからの選択は未対応です（ロードマップ参照）。

### 追加 — 名前付きコード進行・拡張和音・分数コード（`0f4bbef`）

複数の実務者資料・実曲データで裏付けが取れたコード進行を、実際に生成・演奏できるようにしました。

- **名前付き進行カタログ（`music/progressions.ts`）**: 王道進行（4536）・小室進行（6451）・丸サ進行（4361） / Just the Two of Us・カノン進行・循環コード（1625）など33種。`GeneratorSettings.progressionId` で指定できます。
- **テンション（9th / 11th / 13th）**: `ChordQuality` を拡張し、7thと9thを同時に持つ和音（maj9等）を表現可能に。アヴォイドノート（メジャー7thの自然11th）は自動で#11に回避します。
- **分数コード（スラッシュベース）**: `ChordEvent.bass` で、和音の構成音でないベース音を指定可能に（ブラックアダーコード `Vaug/♭II` など）。
- **クリシェ・経過減七・トライトーン代理・バックドア進行**などの装飾技法を追加。

**実装中に見つけて修正したバグ**:
- 変化記号付きの根音（♭VII等）が、変化前の度数のダイアトニック品質を誤って引き継いでいた
- 一部の非ダイアトニック和音が `source: "diatonic"` と誤分類されていた
- 保存したプロジェクトJSONに、新しく追加した特殊和音の種別（`passingDiminished` / `chromatic`）やテンション・分数ベースの情報が入っていると、**再読込時に拒否される**バグがあった（`json.ts` のスキーマ検証が未対応だったため）。全33進行のJSON往復を確認済み。

**検証**: 169テスト（新規23件）が通過。typecheck・lint・build・OpenAPI型契約すべてクリーン。

コード進行・拡張和音・ジャンル別和声の調査根拠は [docs/research/](docs/research/) にまとめています。

## 2026-07-24

- Windows版セットアップの `.ps1` 実行がPowerShell実行ポリシーでブロックされる問題への回避手順をREADMEに追記

### 変更 — プロジェクト名の統一とMITライセンス追加（`5709ad7`）

- ユーザーが目にする文言（画面タイトル・MIDIトラック名・書き出しファイル名・APIタイトル等）を「Visual studio chord」に統一。環境変数プレフィックス（`MTC_`）・ストレージキー・内部識別子はDocker/CI/既存`.env`への影響を避けるため意図的に変更していません。
- `LICENSE`（MIT）を追加し、`package.json` / `pyproject.toml` / READMEにライセンス表記を追加。

## 2026-07-23 以前

- ケイデンス（終止形）の理論的精度向上：短調・モーダルキーで導音を伴わない不完全なV-I終止を修正
- フレーズ構造の改善：不自然に短いフレーズ分割を修正
- README を「何ができるアプリか」から始まる構成に再編
